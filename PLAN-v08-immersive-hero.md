# 海洋漂流瓶 v0.8 高清主视觉计划

## 目标

把应用主界面重新靠近已生成图标的电影感画面：星空、黄昏、白色灯塔、礁石小岛、远船、海面反光和漂流瓶都来自同一套视觉语言。海面要有真实可见的动态，画面不能再依赖 140 x 249 的低清 JPG。

## 当前素材判断

- 可用高清主视觉：`assets/hero-ocean-icon-scene.png`，941 x 1672，约 2.4MB。
- 图标源图：`assets/app-icon-1024.png`，1024 x 1024，约 1.7MB。
- 不再作为主视觉使用：`assets/hero-ocean-icon-scene.jpg`，140 x 249，是上一版画质糊的主要来源。

## 实现方案

1. 用 `assets/hero-ocean-icon-scene.png` 做主背景。
2. 隐藏旧 CSS 拼出来的灯塔、船、海岸和额外瓶子，避免和主视觉重叠。
3. 保留 `.sea` 作为透明动态层，叠加在主视觉海面区域：
   - 细浪线横向漂移
   - 夕阳反光缓慢呼吸
   - 漂流瓶周围水纹微动
   - 海面星点闪烁
4. 用 `.ocean-scene::before` 叠加星光和灯塔光扫动，不遮掉原图细节。
5. 用 `.ocean-scene::after` 增加底部海面暗角和暖色反光，让按钮区域清晰但不变脏。
6. 手机端优先：主图 cover 全屏，保证 390 x 844、430 x 932 上完整沉浸。
7. 桌面端：保持图像中心视觉，同时用渐变和动态海面扩展到宽屏，避免简单拉伸成糊图。

## 发布策略

本地原型可以直接使用高清 PNG。线上 GitHub Pages 目前还缺少该 PNG，因为当前 GitHub 工具只能方便提交文本和小资源；2.4MB 二进制文件需要另一个发布路径：

- 首选：用户同意后用 GitHub 网页上传高清 PNG 到 `assets/`。
- 备选：用本地 git 推送，但当前系统 `git` 被 Xcode license 卡住，需要用户在 Terminal 里同意 Xcode license 后才能使用。
- 备选：后续接入可控的图片 CDN 或部署平台。

在高清 PNG 成功上线前，不应把线上 CSS 强制切到依赖该 PNG 的版本，否则公开站点会缺背景图。

## 验证标准

- 背景不再引用 `hero-ocean-icon-scene.jpg`。
- 手机端能看到高清图标同款主视觉。
- 海面有连续动态：水纹、反光、波纹至少三类动效。
- 灯塔、礁石、小岛、瓶子没有明显重叠或半透明穿帮。
- 文字和按钮在手机、桌面都清楚，不遮住主要视觉焦点。
- 本地刷新后仍然加载正常。
